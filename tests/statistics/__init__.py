"""Necessary to make Python treat the tests directory as a module.

This is required since mypy doesn't support the same file name otherwise
It is also required to enable module specific overrides in pyproject.toml
"""
